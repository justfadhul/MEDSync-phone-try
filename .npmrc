# Expo / React Native (Metro) cannot resolve pnpm's default isolated,
# symlinked node_modules. Expo officially recommends a hoisted layout for
# pnpm monorepos so Metro resolves modules (e.g. NativeWind's
# react-native-css-interop runtime) the same way npm/yarn expose them.
node-linker=hoisted
